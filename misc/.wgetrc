# ~/.wgetrc

# set user agent: IE 9 on Windows 7
user_agent = Mozilla/5.0 (compatible; MSIE 9.0; Windows NT 6.1; Trident/5.0)

# send your email address in a `From:` header with your request
#header = From: Your Name <username@site.domain>

# setup other headers, like `Accept-Language`
#header = Accept-Language: en

# try IPv6 addresses first
#prefer-family = IPv6

# use proxy & the defaults
#use_proxy = on
#https_proxy = http://proxy.site.com:18023/
#http_proxy = http://proxy.site.com:18023/
#ftp_proxy = http://proxy.site.com:18023/

# tune HTTPS security (auto, SSLv2, SSLv3, TLSv1, PFS)
secureprotocol = auto

# wait between every connection
wait = 0

# wait 60s before timing out (default: 15m)
timeout = 60

# retry even when the connection was refused
retry_connrefused = on

# retry a few times but don’t overdo it (default: 20)
tries = 3

# wait up to 5s between retries
waitretry = 5

# ignore `robots.txt` and `<meta name=robots content=nofollow>`
robots = off

# turn on recursive retrieving by default
#recursive = on

# set the maximum depth of the recursive retrieval (default: 5)
reclevel = 5

# force creating directory structure
#dirstruct = on

# use the last component of a redirection URL for the local file name
trust_server_names = on

# use the server-provided last modification date, if available
timestamping = on

# customize the retrieval outlook (default, binary, mega, micro)
#dot_style = default
